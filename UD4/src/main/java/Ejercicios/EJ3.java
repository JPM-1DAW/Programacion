package Ejercicios;

public class EJ3 {
    public static void main(String[] args) {
        int j = -2;
        boolean b = (j > 0) && (1 / (j + 2) > 10);
        System.out.println(b); // false
        // nombre completo
        // nombreCompleto
        // nombre_completo
        // NOMBRE_COMPLETO
        // Nombre Completo
        // nombreCompleto
    }
}
